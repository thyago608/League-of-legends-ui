import styled from "styled-components";

export const Container = styled.main`
  .white-bar {
    background: white;
    height: 60px;
  }

  .container-text {
    background: var(--blue);
    height: 550px;
  }
`;
